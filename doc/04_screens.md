# 04. 画面仕様

## 画面一覧

| 画面名 | URL | アクセス権限 | ファイル |
|---|---|---|---|
| ダッシュボード | `/` | 全ロール | `src/app/page.tsx` |
| 顧客一覧 | `/customer` | 全ロール | `src/app/(page)/customer/index.tsx` |
| 顧客登録 | `/customer/new` | 管理者・営業 | `src/app/(page)/customer/new/index.tsx` |
| 顧客詳細 | `/customer/[cid]` | 全ロール | `src/app/(page)/customer/[cid]/index.tsx` |
| 顧客編集 | `/customer/[cid]/edit` | 管理者・営業（担当者） | `src/app/(page)/customer/[cid]/edit/index.tsx` |
| 商談一覧 | `/deal` | 全ロール | `src/app/(page)/deal/index.tsx` |
| 商談登録 | `/deal/new` | 管理者・営業 | `src/app/(page)/deal/new/index.tsx` |
| 商談詳細 | `/deal/[did]` | 全ロール | `src/app/(page)/deal/[did]/index.tsx` |
| 商談編集 | `/deal/[did]/edit` | 管理者・営業（担当者） | `src/app/(page)/deal/[did]/edit/index.tsx` |
| ユーザー設定 | `/settings` | 全ロール | `src/app/(page)/settings/index.tsx` |
| ユーザー管理 | `/admin/users` | 管理者のみ | `src/app/(page)/admin/users/index.tsx` |
| 初回表示名設定 | `/setup` | 未セットアップユーザー | `src/app/(page)/setup/index.tsx` |
| 権限付与待ち | `/pending` | 未割り当てユーザー | `src/app/(page)/pending/index.tsx` |

---

## ページコンポーネント構造

メイン画面はすべて**外側/内側の2層構造**を採用している。  
詳細は [05_auth_flow.md](./05_auth_flow.md) の「ページコンポーネントの構造パターン」を参照。

```
外側コンポーネント（例: CustomerListPage）
  └── <MainLayout>        ← 認証ガード。state !== 'ok' の間は children を描画しない
        └── <CustomerListContent />  ← データ取得 useEffect はここに書く
```

> **新しいメイン画面を追加する際は必ずこの構造に従うこと。**  
> 外側コンポーネントで `useEffect` によるデータ取得を行うと、  
> `useAuthGuard` より先にデータ API が呼ばれて 403 → ログイン遷移が発生する。

---

## ナビゲーション構成

共通サイドバー（`MainLayout`）がすべてのメイン画面に表示される。

管理者の場合:
```
┌─────────────────────┐
│ CRM                 │
│ 表示名 or email     │
├─────────────────────┤
│ 📊 ダッシュボード   │  → /
│ 👥 顧客一覧        │  → /customer
│ 🤝 商談一覧        │  → /deal
│                     │
├─────────────────────┤
│ 🔐 ユーザー管理    │  → /admin/users  ← 管理者のみ表示
│ ⚙  設定            │  → /settings
│ 🚪 ログアウト      │  → /login（セッション破棄）
└─────────────────────┘
```

管理者以外の場合:
```
┌─────────────────────┐
│ CRM                 │
│ 表示名 or email     │
├─────────────────────┤
│ 📊 ダッシュボード   │  → /
│ 👥 顧客一覧        │  → /customer
│ 🤝 商談一覧        │  → /deal
│                     │
├─────────────────────┤
│ ⚙  設定            │  → /settings
│ 🚪 ログアウト      │  → /login（セッション破棄）
└─────────────────────┘
```

- デスクトップ（sm以上）: 幅220pxの固定サイドバー
- モバイル（xs）: ハンバーガーメニュー → 一時的ドロワー
- 現在のページのメニュー項目がハイライト
- 「ユーザー管理」は `info.isAdmin === true` の場合のみ表示

---

## 各画面の詳細

### ダッシュボード（`/`）

**目的**: システム全体のサマリーを一覧表示。

**表示内容**:
- サマリーカード3枚
  - 取引中顧客数（`status === 'active'`）
  - 進行中商談数（`closed_won` / `closed_lost` 以外）
  - 進行中商談の合計金額
- クイックアクション: 顧客一覧・顧客登録・商談一覧・商談登録
- 直近商談テーブル（クローズ予定日順 5件）

---

### 顧客一覧（`/customer`）

**目的**: 全顧客のリスト表示・登録画面へのナビゲーション。

**表示内容**:
- 顧客テーブル（会社名・業種・ステータス・担当者表示名）
- ステータスは色付きChipで表示
- 行クリックで顧客詳細へ遷移
- 「顧客登録」ボタン（管理者・営業のみ）

---

### 顧客詳細（`/customer/[cid]`）

**目的**: 顧客情報・担当者・対応履歴・関連商談をまとめて管理。

**表示内容**:
- 顧客基本情報カード
  - 担当者欄: ユーザー一覧 Select によるインライン編集（編集アイコン → Select + 保存/取消）
- タブ切り替え: 「担当者」「対応履歴」「商談」
- 担当者タブ: 一覧表示 + 登録/編集/削除モーダル
  - 登録: ユーザー一覧 Select → 選択時に姓・名・カナ・部署・役職・メール・電話・携帯を自動補完
  - 編集: 各フィールドを直接入力
- 対応履歴タブ: 一覧表示 + 登録/編集/削除モーダル
- 商談タブ: `customer_uri` でフィルタした商談一覧（商談名・フェーズ・金額・予定クローズ日）+ 「商談登録」ボタン
- 「商談登録」ボタン（ヘッダー）: `/deal/new?customer={cid}` へ遷移（顧客選択済み・固定状態）
- 「編集」「削除」ボタン（権限に応じて表示）

---

### 顧客登録・編集（`/customer/new`, `/customer/[cid]/edit`）

**目的**: 顧客情報のフォーム入力。

**共通コンポーネント**: `CustomerForm.tsx`

**フィールド**: 会社名（必須）・カナ・業種・企業規模・電話・FAX・郵便番号・住所・Web・ステータス（必須）・獲得経路・年間売上・メモ

> 担当者（`assigned_uid`）は顧客詳細画面のインライン編集からのみ変更可。登録・編集フォームには含まない。

---

### 商談一覧（`/deal`）

**目的**: 全商談のリスト表示。

**表示内容**:
- 商談テーブル（商談名・フェーズ・金額・予定クローズ日・顧客名）
- フェーズは色付きChipで表示
- 行クリックで商談詳細へ遷移

---

### 商談詳細（`/deal/[did]`）

**目的**: 商談情報と関連する対応履歴の表示。

**表示内容**:
- 商談基本情報（金額・フェーズ・確度・クローズ日・担当者）
- 関連顧客へのリンク
- この商談に紐付く対応履歴一覧

---

### 商談登録・編集（`/deal/new`, `/deal/[did]/edit`）

**共通コンポーネント**: `DealForm.tsx`

**フィールド**: 商談名（必須）・顧客（必須・Select）・担当者（Select）・フェーズ（必須）・金額・確度・予定クローズ日・メモ

**顧客選択**:
- 顧客は Select ドロップダウンから選択（`fetchAllCustomers` で全件取得）
- 顧客詳細から遷移した場合（`?customer={cid}`）: 顧客が選択済みかつ変更不可（disabled）、キャンセルで元の顧客詳細に戻る

**担当者選択**:
- 顧客選択後に有効化（顧客未選択・ロード中は disabled）
- 顧客に紐付く担当者一覧から Select。顧客変更時に自動リセット

---

### ユーザー設定（`/settings`）

**目的**: 自分のアカウント情報確認とプロフィール編集。

**表示内容**:
- アカウント情報（UID・メールアドレス・現在の権限Chip）
- プロフィールフォーム + 保存ボタン
  - 表示名（必須）・姓・名・姓カナ・名カナ・部署・役職・電話番号・携帯番号

> プロフィールフィールドは `contact`（顧客担当者）エンティティと共通。担当者登録時にここで入力した情報が自動補完される。  
> 保存時にログインメールアドレス（`email`）も userprofile に自動書き込みされ、担当者選択時の補完に使われる。

---

### ユーザー管理（`/admin/users`）

**目的**: 管理者がユーザーに営業担当者・閲覧者権限を付与・剥奪する。

**表示内容**:
- ユーザーテーブル（UID・表示名・営業担当者列・閲覧者列）
- 権限あり: カラーChip（`×` ボタンで削除）
- 権限なし: `+` アイコンボタン（クリックで付与）
- 操作は即時反映

---

### 初回表示名設定（`/setup`）

**目的**: 初回ログイン時に表示名を設定させる。

**条件**: `display_name` が未設定のユーザーが対象。

**フロー**:
1. 表示名を入力して「次へ」
2. 保存後、権限の有無に応じてリダイレクト
   - 権限あり → `/`（ダッシュボード）
   - 権限なし → `/pending`（権限付与待ち）

---

### 権限付与待ち（`/pending`）

**目的**: 権限が付与されるまでユーザーを待機させる。

**条件**: `display_name` 設定済みだがロールなしのユーザーが対象。

**機能**:
- 「管理者に権限付与を依頼してください」メッセージ
- カウントダウン（10秒）付き自動ポーリング
- 「今すぐ確認する」ボタン
- 権限が付与されると自動的にダッシュボードへ遷移
